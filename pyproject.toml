[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tokamak-sizing"
version = "0.1.0"
description = "Outside-in tokamak reactor sizing tool"
requires-python = ">=3.10"
dependencies = ["numpy", "matplotlib"]
